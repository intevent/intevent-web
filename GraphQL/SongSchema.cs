using GraphQL.Types;

namespace intevent_web.GraphQL
{
    public class SongSchema : Schema
    {/* 
        public SongSchema(IParty party) 
        {
            Query = new ChatQuery(chat);
            Mutation = new ChatMutation(chat);
            Subscription = new PartySubscriptions(party);
        }*/
    }
}
